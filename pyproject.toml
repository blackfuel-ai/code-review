[project]
name = "code-review"
version = "0.1.0"
description = "AI-powered PR code review GitHub Action by Blackfuel."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = ["pytest>=9.1.0"]
